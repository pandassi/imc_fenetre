from mod_bmi_modele import Bmi


def calcul_imc(unpatient: Bmi): #voit pas Bmi donc l'importer (copier coller)
    return unpatient.poids / (unpatient.taille **2) #reste à importer dans l'autre fenêtre